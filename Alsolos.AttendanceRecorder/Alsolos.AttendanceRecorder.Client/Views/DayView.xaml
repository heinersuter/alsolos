<UserControl x:Class="Alsolos.AttendanceRecorder.Client.Views.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Alsolos.AttendanceRecorder.Client.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance views:DayViewModel}">

    <Expander>
        <Expander.Header>
            <StackPanel Orientation="Horizontal" Height="32">
                <TextBlock Text="{Binding Date, StringFormat=D}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalTime}" Margin="16 0 0 0" VerticalAlignment="Center"/>
            </StackPanel>
        </Expander.Header>
        <ItemsControl ItemsSource="{Binding Intervals}" HorizontalContentAlignment="Stretch" Grid.IsSharedSizeScope="True" Margin="4">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <views:IntervalView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Expander>

</UserControl>
