<UserControl x:Class="Alsolos.AttendanceRecorder.Client.Views.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Alsolos.AttendanceRecorder.Client.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance views:DayViewModel}">

    <Expander>
        <Expander.Header>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Date, StringFormat=D}"/>
                <TextBlock Text="{Binding TotalTime}" Margin="16 0 0 0"/>
            </StackPanel>
        </Expander.Header>
        <ListBox ItemsSource="{Binding Intervals}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <views:IntervalView/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Expander>

</UserControl>
